import { ContactPageSkeleton } from '@/components/ui/page-skeletons';

/**
 * Loading component for Contact Us page
 * Shows a skeleton that matches the exact layout of the ContactUsClient component
 */
export default function ContactUsLoading() {
  return <ContactPageSkeleton />;
}
