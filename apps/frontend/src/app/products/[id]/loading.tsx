import { ProductDetailPageSkeleton } from '@/components/ui/page-skeletons';

/**
 * Loading component for Product Detail page
 * Shows a skeleton that matches the exact layout of the ProductDetailClient component
 */
export default function ProductDetailLoading() {
  return <ProductDetailPageSkeleton />;
}
