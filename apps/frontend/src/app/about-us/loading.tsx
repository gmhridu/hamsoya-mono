import { AboutUsPageSkeleton } from '@/components/ui/page-skeletons';

/**
 * Loading component for About Us page
 * Shows a skeleton that matches the exact layout of the AboutUsPage component
 */
export default function AboutUsLoading() {
  return <AboutUsPageSkeleton />;
}
